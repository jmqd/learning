import logging

def quicksort(array, length, pivot = 1):
    logging.info('Sorting: length = {}, pivot = {}'.format(length, pivot))
    part = partition() # still needs inputs here

def partition(array, length, pivot):
    logging.info('Partitioning: length = {}, pivot = {}'.format(length, pivot))

quicksort(3, 2, 1)
